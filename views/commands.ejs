<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Aneo | Commands</title>
    </head>

    <body>
        <a href="/"><img class="avatar" src="https://cdn.discordapp.com/avatars/811265195186978828/fbeed51a370d1c957623c542f5bceeb2.png" alt="aneo-avatar"></a>
        <section class="main-content">
            <p>use h!help in a server to get all the commands </p>

            <% commands.forEach(({ name, value }) => { %>
                <table style="width: 100%">
                    <h1 style="text-align: center;">
                        <%= name %>
                    </h1>
                    <tr>
                        <th>Command Name</th>
                        <th>Description</th>
                        <th>Aliases</th>
                    </tr>
                    <% value.forEach(({ name, description, aliases, usage }) => { %>
                        <tr>
                            <td><%= name%></td>
                            <td><%= description%></td>
                            <td><%= typeof aliases === 'string' ? aliases : aliases.join(', ')%></td>
                        </tr>
                    <%}) %>
                </table>
            <%}) %>
        </section>
    </body>

<style>
    table {
        border-collapse: collapse;
        border-spacing: 0;
    }
    * {
        box-sizing: border-box;
    }

    body {
        padding: 0;
        margin: 0;
        font-family: "Open Sans";
        font-size: 60px;
        line-height: 1.5;
        color: darkblue;
    }

    .main-content {
        word-wrap: break-word;
    }

    @media screen and (min-width: 64em) {
        .main-content {
            max-width: 64rem;
            padding: 2rem 6rem;
            margin: 0;
            font-size: 1.1rem;
            }
    }

    @media screen and (min-width: 42rem) and (min-width: 64em) {
        .main-content {
            padding: 2rem 4rem;
            font-size: 1.1rem;
            }
    }

    @media screen and (min-width: 42em) {
        .main-content {
            padding: 2rem 1rem;
            font-size: 1rem;
            }
    }

    .main-content h1,
    .main-content h2,
    .main-content h3,
    .main-content h4,
    .main-content h5,
    .main-content h6 {
        margin-top: 2rem;
        margin-bottom: 1rem;
        font-weight: normal;
        color: rgba(60, 47, 177, 0.6);
    }

    .main-content table th,
    .main-content tabe td {
        padding: 0.5rem 1rem;
        border: 1px solid snow;
    }

    .avatar {
        display: block;
        margin-left: auto;
        margin-right: auto;
        border: 4px solid blueviolet;
        border-radius: 100%;
        width: 20%;
        height: 20%;
        -webkit-transform: -webkit-transform .8s ease-in-out;
        transition: transform .8s ease-in-out;
    }
    .avatar:hover{
        -webkit-transform: rotate(360deg);
        transform: rotate(360deg);
    }
</style>
</html>